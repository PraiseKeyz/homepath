import { PropertyMarketplace } from "../properties/_components/property-marketplace";

export const metadata = { title: "HomePath — Browse Homes" };

export default function DashboardHomePage() {
  return <PropertyMarketplace />;
}
